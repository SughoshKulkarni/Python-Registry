# Python-Registry
A reference implementation of the Python Registry Pattern. Demonstrates how to replace hard-coded conditionals with decoupled dictionary lookups and self-registering decorators for adherence to the Open/Closed Principle.
